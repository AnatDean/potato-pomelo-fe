import styled from 'styled-components';
import { myTheme } from '../my-theme';

export const Card = styled.li`
  width: 85vw;
  height: 20vh;
  background-color: white;
  border: solid 1px ${myTheme.colors.neutral};
`;
